import React, { FC, useState, FormEvent } from 'react';
import { useDispatch } from 'react-redux';

interface SearchProps {
    title: string;
}


/** Search query to find cities and countries */
const Search: FC<SearchProps> = ({title}) => {

    return(
        <div>
      
    </div>
    )
}
export default Search;